apiVersion: v1
kind: Pod
metadata:
  name: Restart-onfail
spec: 
  restartPolicy: OnFailure
  containers:
    - name: OnFailure
      image: alpine
      args:
        - sleep
        - 3sm

#----------------------------------------- 

apiVersion: v1
kind: Pod
metadata:
  name: Restart-success
spec:
  restartPolicy: OnFailure
  containers:
    - name: onfailure
      image: alpine
      args:
        - sleep
        - 3s
