apiVersion : v1
kind : Pod
metadata :
  name : ngnixhttpd-pod
spec :
  container :
    - name : nginx-pod
      image : nginx
      ports :
        - containerPort : 80
  container :
    - name : httpd-pod
      image : httpd
      ports :
        - containerPort : 80         
