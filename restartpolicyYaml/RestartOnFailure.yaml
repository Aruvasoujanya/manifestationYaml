apiVersion: v1
kind: Pod
metadata:
  name: restart-success
spec:
  restartPolicy: OnFailure
  containers:
    - name: onfailure
      image: alpine
      args:
        - sleep
        - 3s

#----------------------------------------- 

apiVersion: v1
kind: Pod
metadata:
  name: restart-onfail
spec: 
  restartPolicy: OnFailure
  containers:
    - name: OnFailure
      image: alpine
      args:
        - sleep
        - 3sm
