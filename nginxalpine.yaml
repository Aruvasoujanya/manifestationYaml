apiVersion : v1
kind : Pod
metadata :
  name : exec 
spec:
  containers :
    - name : nginx
      image : nginx:1.14.2
      ports :
        - containerPort : 80
  containers :
    - name : alpine
      image : alpine
      args :
        - sleep
        - 1d
     
     