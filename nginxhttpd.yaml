apiversion : v1
kind : Pod
metadata :
  name : ngnixhttpd-pod
spec :
  container :
    - image : nginx
      name : nginx-pod
      ports :
        - containerPort : 80
  container :
    - image : httpd
      name : httpd-pod
      ports :
        - containerPort : 80         
