cdapiversion : v1
kind : Pod
metadata :
  name : exec 
spec:
  container :
    - name : nginx
      image : nginx:1.14.2
      ports :
        - containerPort : 80
  container :
    - name : alpine
      image : alpine
      args :
        - sleep
        - 1d
     
     