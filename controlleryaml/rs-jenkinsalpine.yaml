apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: jen-alpi-rs
spec:
  minReadySeconds: 5
  replicas: 5
  selector:
    matchLabels: 
      app: jenkins
  template:
    metadata:
      name: jen-alpi1
      labels:
        app: jenkins
    spec:
      containers:
        - name: jenkin
          image: jenkins/jenkins:lts-jdk17
          ports:
            - containerPort: 8080
        - name: alpin
          image: alpine:3
          command:
            - sleep
            - 1d   
            
---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: jenkins
spec:
 minReadySeconds: 2
 replicas: 5
 selector:
   matchLabels:
     app: jenkins
 template:
   metadata:
     name: jenkins
     labels:
       app: jenkins
   spec:
     containers:
       - name: jenkins
         image: jenkins
         ports: 
           - containerPort: 8080



